{
  "name": "@verifrax/verifrax",
  "private": false,
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "ci": "pnpm -r test && pnpm -r lint",
    "build": "pnpm -r --if-present build",
    "build:ci": "pnpm -r --if-present build",
    "build:strict": "pnpm -r --if-present build",
    "test": "pnpm -r --if-present test",
    "lint": "pnpm -r --if-present lint"
  },
  "devDependencies": {
    "@types/node": "26",
    "@vercel/ncc": "^0.45.0",
    "typescript": "^7.0.2"
  },
  "dependencies": {
    "@sigstore/verify": "4.1.2"
  },
  "engines": {
    "node": "^24.15.0"
  },
  "bin": {
    "verifrax-seal": "scripts/verifrax-seal.js"
  },
  "version": "0.1.4",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Verifrax/VERIFRAX.git"
  },
  "homepage": "https://github.com/Verifrax/VERIFRAX#readme",
  "bugs": {
    "url": "https://github.com/Verifrax/VERIFRAX/issues"
  },
  "publishConfig": {
    "access": "public"
  },
  "files": [
    "core",
    "verifier",
    "verifrax-engine",
    "profiles",
    "policy",
    "README.md",
    "LICENSE"
  ]
}
