# VERIFRAX_EXECUTION_V1 — OPERATIONAL COMMAND SET

**MODE:** MANUAL · IRREVERSIBLE · SINGLE-SHOT
**AUTHORITY:** ACTIVE
**SCOPE:** FIRST PAID EXECUTION ONLY

---

## CMD-001 — PRODUCT LOCK

```
SET PRODUCT_ID=VERIFRAX_EXECUTION_V1
SET EXECUTION_MODE=DETERMINISTIC
SET EXECUTION_COUNT=1
SET REVISION=DISABLED
SET DISPUTE=DISABLED
```

```
ON INQUIRY:
RESPOND EXACTLY:
"SINGLE deterministic verification execution.
ONE execution → ONE final certificate.
NO revision. NO re-run. NO dispute."
```

---

## CMD-002 — PRICE LOCK

```
SET PRICE_EUR=650
SET CURRENCY=EUR
SET DISCOUNT=0
SET BUNDLES=DISABLED
SET NEGOTIATION=DISABLED
```

```
IF COUNTEROFFER_DETECTED=true
→ TERMINATE_THREAD
```

---

## CMD-003 — PAYMENT FLOW (BANK TRANSFER ONLY)

```
SET PAYMENT_METHOD=BANK_TRANSFER
SET STRIPE=DISABLED
SET AUTOMATION=OFF
SET CHARGEBACK_SURFACE=ZERO
```

### INVOICE_GENERATE()

```
SELLER=VERIFRAX
SERVICE="Deterministic verification execution (single-use)"
PRICE=€650
NOTE="Execution is final and irreversible upon payment."
```

```
IF FUNDS_STATUS != CLEARED
→ BLOCK_EXECUTION
```

---

## CMD-004 — CONSENT GATE

### SEND_CONSENT_MESSAGE()

```
VERIFRAX — EXECUTION CONDITIONS

You are purchasing a single deterministic verification execution.

• One execution → one certificate
• No revision, re-run, or amendment
• No interpretation, advice, or opinion
• Certificate validity is independent of VERIFRAX availability

If you proceed, reply with "ACKNOWLEDGED".
```

```
WAIT FOR RESPONSE
```

```
IF RESPONSE != "ACKNOWLEDGED"
→ TERMINATE_PROCESS
```

---

## CMD-005 — EXECUTION PIPELINE

```
RECEIVE INPUT=bundle.bin
ASSERT INPUT=OPAQUE
```

```
EXECUTE verification(bundle.bin)
```

```
GENERATE certificate.json
COMPUTE certificate_hash
```

### DELIVER_ONLY()

```
OUTPUT_1=certificate.json
OUTPUT_2=certificate_hash
OUTPUT_3=https://www.verifrax.net/verify
```

```
NO_COMMENTARY
NO_INTERPRETATION
NO_FOLLOWUP
```

---

## CMD-006 — TARGET SELECTION

```
SET TARGET_COUNT=1
SET BROADCAST=DISABLED
SET CHANNEL=PRIVATE
```

```
SELECT ONE FROM:
- LAW_FIRM_PARTNER
- COMPLIANCE_OFFICER
- INVESTIGATIVE_JOURNALIST
- FORENSIC_ANALYST
```

---

## CMD-007 — OUTREACH MESSAGE

### SEND_MESSAGE_ONCE()

```
We operate a deterministic verification system.
One execution. Final certificate. No disputes.
€650.

If you have evidence that must survive scrutiny, reply INTERESTED.
```

```
SET FOLLOW_UPS=0
```

```
IF RESPONSE != "INTERESTED"
→ STOP
→ SELECT_NEW_TARGET (only after STOP)
```

---

## CMD-008 — SUCCESS GATE

```
IF PAID_EXECUTIONS >= 1
THEN
  SET PRICE=HARDENED
  SET LEGAL_TEXT=FORMALIZE
  SET AUTOMATION=EVALUATE
ELSE
  SET EXPANSION=DISABLED
  SET FEATURES=DISABLED
  SET STRIPE=DISABLED
```

---

## CMD-009 — GLOBAL ENFORCEMENT RULES

```
PRIORITY:
FINALITY > SCALE
CLARITY > MARKETING
MANUAL > REVERSIBLE
ONE_CLIENT > MANY_LEADS
```

```
ANY VIOLATION
→ TERMINATE_PROCESS
```

---

## CMD-010 — ENTRYPOINT LOCK

```
VALID_START_SEQUENCE:
1 OUTREACH_SENT
2 RESPONSE="INTERESTED"
3 CONSENT="ACKNOWLEDGED"
4 INVOICE_SENT
5 FUNDS_CLEARED
6 EXECUTION_RUN
7 CERTIFICATE_DELIVERED
8 STOP
```

```
IF SEQUENCE_DEVIATION=true
→ TERMINATE_IMMEDIATELY
```

---

## CMD-011 — OPERATOR CHECKLIST (ENFORCED)

```
□ TARGET_SELECTED(1)
□ OUTREACH_SENT(EXACT)
□ RESPONSE=INTERESTED
□ CONSENT=ACKNOWLEDGED
□ INVOICE_SENT
□ FUNDS_CLEARED
□ BUNDLE_RECEIVED
□ EXECUTION_COMPLETED
□ CERTIFICATE_GENERATED
□ HASH_COMPUTED
□ VERIFY_REFERENCE_CONFIRMED
□ CERTIFICATE_DELIVERED
□ PAID_EXECUTIONS += 1
□ STOP
```

---

## CMD-012 — ROOT AUTHORITY FREEZE

```
SET ACTIVE_PRODUCT=VERIFRAX_EXECUTION_V1
SET OTHER_PRODUCTS=DISABLED
```

```
NO_PARALLEL_IDEAS
NO_EXPERIMENTS
NO_VARIANTS
```

---

**SYSTEM STATE:**
READY → SELECT_TARGET=1 → SEND_MESSAGE → WAIT

---

**AUTHORITY:** This command set is the operational interface for VERIFRAX_EXECUTION_V1.

**UPDATES:** No updates without explicit version increment.

