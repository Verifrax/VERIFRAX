# VERIFRAX Policy Snapshot v1.0.0

Immutable policy snapshot.

- Forward-only: do not modify in place.
- New policy versions must be added as new directories: `policy/vX.Y.Z/`.

Files:
- `policy.yaml` — deterministic policy definition.

