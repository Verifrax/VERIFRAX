> This document is for educational purposes. Active release freeze authority is defined by `release-integrity/freeze-surfaces.json`. Normative protocol semantics are defined under `docs/spec/`.

# Evidence Bundle

An evidence bundle is the canonical structured input to VERIFRAX verification.

It contains the material required for deterministic evaluation, including evidence payloads, declared metadata, authority-linked fields, and any verification-relevant attachments or references defined by the normative specification.

This document is explanatory and does not override the normative specification.
