> This document is for educational purposes. Active release freeze authority is defined by `release-integrity/freeze-surfaces.json`. Normative protocol semantics are defined under `docs/spec/`.

# Edge Verification Interface

This document describes the edge-facing verification interface at a high level.

It is intended to help readers understand how VERIFRAX verification may be exposed operationally, but it is not the normative protocol definition.

Normative behavior, required semantics, and conformance expectations are defined by the specification and maintained conformance surfaces.
