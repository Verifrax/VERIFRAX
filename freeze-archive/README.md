# freeze-archive

This directory is historical only.

Canonical freeze surface lives in `freeze/` and is defined by:
- `freeze/FREEZE_SURFACE_MANIFEST.json`

CI MUST NOT treat `freeze-archive/` as canonical.
Any new versioned freeze release directory belongs here.
