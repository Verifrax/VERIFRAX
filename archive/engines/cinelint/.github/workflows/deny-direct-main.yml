name: Deny Direct Main Push
on:
  push:
    branches: [main]
jobs:
  deny:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "BLOCKED: direct push to main forbidden. Use PR."
          exit 1
