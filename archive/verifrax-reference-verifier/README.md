# Historical Reference Verifier Archive

This directory preserves the earlier `verifrax-reference-verifier` repository surface for historical continuity, audit lineage, and implementation traceability.

It is not an active reference implementation.
It does not define current verifier authority.
It must not be used as the canonical execution target for active protocol conformance.

## Active verifier authority

The active maintained verifier surfaces are:

- `verifier/node`
- `verifier/rust`

Those directories are the current repository authority for maintained reference verifier implementations.

## Status

- Classification: archival
- Maintenance state: inactive
- Normative authority: none
- Operational role: historical preservation, lineage inspection, audit support

## Interpretation rule

Implementers, auditors, operators, and downstream integrators must target the maintained verifier surfaces under `verifier/` and must not rely on this archived directory for current conformance or release alignment.

## Scope note

This archive may contain prior verifier logic, transitional layouts, earlier packaging assumptions, or superseded execution surfaces retained only for repository history.
