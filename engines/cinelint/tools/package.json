{
  "name": "@verifrax/tools",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "node bundle-verify.mjs --help >/dev/null || exit 0",
    "verify:bundle": "node bundle-verify.mjs",
    "lint": "echo 'lint: no linter configured' && exit 0",
    "test": "echo 'test: no tests configured' && exit 0"
  }
}
