PILOT v1 CHECKLIST

1) Receive one real delivery package request (inputs + profile).
2) Run pipeline and generate bundle per bundle.v1 spec.
3) Customer runs verifier locally: VERIFIED required.
4) Customer signs acceptance line item tied to certificate_id + hash.
5) Invoice SKU-A2 for the certificate.

