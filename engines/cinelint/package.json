{
  "name": "verifrax",
  "private": true,
  "workspaces": [
    "apps/*",
    "core",
    "nodes/*",
    "sdk/*",
    "ui"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit --skipLibCheck",
    "lint": "echo 'lint: no linter configured' && exit 0",
    "format": "echo 'format: no formatter configured' && exit 0",
    "build": "pnpm -w -r build",
    "test": "./scripts/test.sh",
    "fuzz": "./scripts/fuzz.sh",
    "formal": "./scripts/formal.sh"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
